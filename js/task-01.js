'use strict';

const logItems = ['Mango', 'Poly', 'Ajax', 'Lux', 'Jay', 'Kong'];
for (let i = 1; i < logItems.length; i++) {
  console.log(`${i} - ${logItems[i]}`);
}

// logItems([5, 10, 15, 20, 25, 30, 35, 40, 45, 50]);
